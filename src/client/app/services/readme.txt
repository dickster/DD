TODO :
i should follow the packaging pattern where you group files by function, not type.
e.g. all files related to policies are in one folder, (the component to view them, the service to read them, etc...)
and then create a module that groups them all together

(see shared.module.ts for example).
